<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   <mapper namespace="mybatis.MaterialMapper">
    <select id="materialSelect1" parameterType="int" resultType="Material" >
    select * from [Material] where id = #{id}
    </select>
    
    <select id="materialInsert" parameterType="Material"  statementType="CALLABLE">
    <![CDATA[
   {
   Call uspMaterialCreate(
    #foreach($column in $columns)
    #if($velocityCount==$columns.size())   
     #{$column.getCOLUMN_NAMEFL(),jdbcType= $column.TYPE_NAME}
    #else
     #{$column.getCOLUMN_NAMEFL(),jdbcType=$column.TYPE_NAME}, 
    #end
    #end
   
   
   #{materialName,jdbcType=NVARCHAR}, 
   #{materialQuantity,jdbcType=INTEGER}, 
   #{applicableModels,jdbcType=NVARCHAR}, 
   #{materialPrice,jdbcType=DECIMAL}, 
   #{customer,jdbcType=NVARCHAR}, 
   #{leader,jdbcType=NVARCHAR},
   #{remark,jdbcType=NVARCHAR}, 
   #{entryTime,jdbcType=TIMESTAMP}, 
   #{outTime,jdbcType=TIMESTAMP})
      }
       ]]>
    </select>
    
     <select id="materialSelect" resultType="MaterialSearch" parameterType="Material"  statementType="CALLABLE">
    <![CDATA[
   {
   Call uspMaterialListGet()
      }
       ]]>
    </select> 
    
    
    
    
    <select id="userValidate"  parameterType="User" resultType="boolean"  statementType="CALLABLE">
  
    <![CDATA[
   {
    Call sysUserValidate(
    #{loginID,jdbcType=NVARCHAR},
    #{password,jdbcType=NVARCHAR}
    )
      }
       ]]>
    </select>
    
   </mapper>