DROP PROCEDURE IF EXISTS `uspProductAvailableUpdate001`;
DELIMITER ;;
CREATE  PROCEDURE `uspProductAvailableUpdate001`(
        )
BEGIN
UPDATE ProductAvailable
SET     WHERE ******* = ********

IF (@@rowcount<>0)
RETURN 0;
END

